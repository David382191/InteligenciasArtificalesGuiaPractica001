python -m pip install pandas
python -m pip install plotly
python -m pip install streamlit


python -m streamlit run interfaz.py

- Me salió un error.
Internal server error: Unable to retrieve the Github ID 
for your workspace. 
Please retry and contact support if it happens again.



python -m pip install numpy

python -m pip install sqlite3
 
python -m pip install matplotlib

import pandas as pd


Tiene que decir, "Filtros de Consulta"
Abajo tiene que tener un "checklist" que tenga la función de 
"Análizar todos los años (2015-2025)"
